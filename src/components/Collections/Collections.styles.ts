import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    box: {
        backgroundColor: 'beige',
        margin: '10px',
        padding: '16px',
        borderRadius: '12px'
    },
    text: {
        fontWeight: 'bold',
        margin: '10px'
    }
}));
